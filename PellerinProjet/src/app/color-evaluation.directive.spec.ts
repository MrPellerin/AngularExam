import { ColorEvaluationDirective } from './color-evaluation.directive';

describe('ColorEvaluationDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorEvaluationDirective();
    expect(directive).toBeTruthy();
  });
});
